spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/MunafBadarpura/hotel-microservices-configurations
          clone-on-start: true #indicate that repo cloned on startup (not on demand).leads to slower startup but faster first query.

server:
  port: 8085


